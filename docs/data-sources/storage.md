---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_storage Data Source - coolify"
subcategory: ""
description: |-
  Retrieves a single persistent storage by UUID from a Coolify application, service, or database.
---

# coolify_storage (Data Source)

Retrieves a single persistent storage by UUID from a Coolify application, service, or database.

## Example Usage

```terraform
# Look up a single persistent storage by UUID
data "coolify_storage" "example" {
  uuid             = "your-storage-uuid"
  application_uuid = "your-app-uuid"
}

output "storage_mount_path" {
  value = data.coolify_storage.example.mount_path
}

output "storage_host_path" {
  value = data.coolify_storage.example.host_path
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `uuid` (String) The UUID of the persistent storage.

### Optional

- `application_uuid` (String) The UUID of the application. Exactly one of `application_uuid`, `service_uuid`, or `database_uuid` must be provided.
- `database_uuid` (String) The UUID of the database. Exactly one of `application_uuid`, `service_uuid`, or `database_uuid` must be provided.
- `service_uuid` (String) The UUID of the service. Exactly one of `application_uuid`, `service_uuid`, or `database_uuid` must be provided.

### Read-Only

- `host_path` (String) The host path (empty for Docker volumes).
- `mount_path` (String) The mount path inside the container.
- `name` (String) The name of the persistent storage.
