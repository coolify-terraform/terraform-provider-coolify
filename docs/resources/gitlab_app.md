---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_gitlab_app Resource - coolify"
subcategory: ""
description: |-
  Manages a Coolify GitLab App source. Requires Coolify >= v4.3.0.
---

# coolify_gitlab_app (Resource)

Manages a Coolify GitLab App source. Requires Coolify >= v4.3.0.

## Example Usage

```terraform
variable "gitlab_app_client_secret" {
  type      = string
  sensitive = true
}

resource "coolify_gitlab_app" "example" {
  name          = "corp-gitlab"
  html_url      = "https://gitlab.example.com"
  client_id     = "gitlab-app-id"
  client_secret = var.gitlab_app_client_secret
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `html_url` (String) GitLab HTML URL (for example https://gitlab.example.com).
- `name` (String) Display name.

### Optional

- `api_url` (String) GitLab API URL. Coolify derives it from html_url when omitted.
- `client_id` (String) OAuth application id.
- `client_secret` (String, Sensitive) OAuth application secret. Preserved when GET omits it.
- `custom_port` (Number) SSH port. Coolify defaults to 22.
- `custom_user` (String) SSH user. Coolify defaults to git.
- `group_name` (String) Optional group filter.
- `is_system_wide` (Boolean) System-wide on self-hosted Coolify only.
- `redirect_uri` (String) OAuth redirect URI. May be a private URL.
- `webhook_token` (String, Sensitive) Webhook secret. Coolify generates one when omitted.

### Read-Only

- `id` (Number) Coolify numeric GitLab App id (used in PATCH/DELETE).
- `uuid` (String) Coolify UUID when returned.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using the Coolify numeric GitLab App id, or the UUID when Coolify returns one.
terraform import coolify_gitlab_app.example 7
```
