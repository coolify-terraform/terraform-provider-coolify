---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_tag Resource - coolify"
subcategory: ""
description: |-
  Manages a Coolify team tag. Requires Coolify >= v4.2.0. Attach tags to resources with coolify_resource_tag.
---

# coolify_tag (Resource)

Manages a Coolify team tag. Requires Coolify >= v4.2.0. Attach tags to resources with `coolify_resource_tag`.

## Example Usage

```terraform
resource "coolify_tag" "frontend" {
  name = "frontend"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Tag name (unique per team, at least 2 characters after Coolify sanitizes it).

### Read-Only

- `uuid` (String) Tag UUID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import coolify_tag.frontend <tag-uuid>
```
