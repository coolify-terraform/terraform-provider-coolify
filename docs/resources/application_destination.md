---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_application_destination Resource - coolify"
subcategory: ""
description: |-
  Attaches an additional standalone Docker destination to an application. Do not use this for the primary destination (destination_uuid on the application resource). Requires Coolify >= v4.2.0.
---

# coolify_application_destination (Resource)

Attaches an additional standalone Docker destination to an application. Do not use this for the primary destination (`destination_uuid` on the application resource). Requires Coolify >= v4.2.0.

## Example Usage

```terraform
resource "coolify_application_destination" "extra" {
  application_uuid = coolify_application.example.uuid
  destination_uuid = coolify_destination.extra.uuid
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `application_uuid` (String)
- `destination_uuid` (String)

### Read-Only

- `id` (String) Composite id application_uuid:destination_uuid.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import coolify_application_destination.extra <application-uuid>:<destination-uuid>
```
