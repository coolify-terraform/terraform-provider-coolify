---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_resource_tag Resource - coolify"
subcategory: ""
description: |-
  Attaches a Coolify tag to an application, database, or service. Requires Coolify >= v4.2.0.
---

# coolify_resource_tag (Resource)

Attaches a Coolify tag to an application, database, or service. Requires Coolify >= v4.2.0.

## Example Usage

```terraform
resource "coolify_tag" "frontend" {
  name = "frontend"
}

resource "coolify_resource_tag" "app" {
  resource_type = "application"
  resource_uuid = coolify_application.example.uuid
  tag_name      = coolify_tag.frontend.name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `resource_type` (String) One of `application`, `database`, or `service`. Changing this forces a new resource.
- `resource_uuid` (String) UUID of the tagged resource. Changing this forces a new resource.
- `tag_name` (String) Tag name to attach (must already exist). Changing this forces a new resource.

### Read-Only

- `id` (String) Composite id `resource_type:resource_uuid:tag_uuid`.
- `tag_uuid` (String) UUID of the attached tag.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import coolify_resource_tag.app application:<application-uuid>:<tag-uuid>
```
